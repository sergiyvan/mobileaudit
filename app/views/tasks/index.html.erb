<h1>Listing tasks</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Address</th>
      <th>Price</th>
      <th>Expirience</th>
      <th>Status</th>
      <th>Task type</th>
      <th>Content</th>
      <th>Exp require</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.name %></td>
        <td><%= task.description %></td>
        <td><%= task.address %></td>
        <td><%= task.price %></td>
        <td><%= task.expirience %></td>
        <td><%= task.status %></td>
        <td><%= task.task_type %></td>
        <td><%= task.content %></td>
        <td><%= task.exp_require %></td>
        <td><%= link_to 'Show', task %></td>
        <td><%= link_to 'Edit', edit_task_path(task) %></td>
        <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Task', new_task_path %>
